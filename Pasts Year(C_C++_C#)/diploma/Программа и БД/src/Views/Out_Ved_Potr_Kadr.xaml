<UserControl x:Class="Programm.Views.Out_Ved_Potr_Kadr"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid>
        <ListView IsSynchronizedWithCurrentItem="True" ItemsSource="{Binding Path=Data}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Структурное подразделение" DisplayMemberBinding="{Binding Path=Podr_Name}"/>
                    <GridViewColumn Header="Должность" DisplayMemberBinding="{Binding Path=Dolj_Name}"/>
                    <GridViewColumn Header="Кол-во штатных единиц по штатному расписанию" DisplayMemberBinding="{Binding Path=Plan_Kolvo}"/>
                    <GridViewColumn Header="Кол-во штатных единиц на учете" DisplayMemberBinding="{Binding Path=Uch_Kolvo}"/>
                    <GridViewColumn Header="Требуемое кол-во чел." DisplayMemberBinding="{Binding Path=Tr_Kolvo}"/>
                    
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
