#include "func.h"
void print ( void )
{
    FILE *in;
    if ((in = fopen( "bank.db" , "r" )==N
    {
        printf("Файла с данными нет");
        exit(1);
    }
    while( !feof( in ) )
    {
        fscanf( in , "%c" , a );
        printf( "%s" , a );
    }
}
void edit ( void)
{
    FILE *in ;
    int i;
    in=fopen ( "bank.db" , "r+" ) ;
    while (!feof (in))
    {
        fscanf(in,"%d", &i);
        fscanf(in,"%s",a);
    }
    printf ( "Введите новую запись :\n" ) ;
    gets ( a ) ;
    gets ( a ) ;
    fprintf ( in , "%d,%s" , i+1 ,a ) ;

}

